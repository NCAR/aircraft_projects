See also nimbus/filters directory.

e.g:

MATLAB commands to generate a filter, plot, and save:

b=firpm(100, [0.0, 0.225, 0.5, 0.0], [1.0, 1.0, 0.0, 0.0]);

freqz(b, 1, 4096);

save('seven', '-ascii', '-double', 'b');

