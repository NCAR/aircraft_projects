#
# cshell startup script
#

source $HOME/ads3_environment.csh

unset noclobber
set clobber
set fignore     = .o
set prompt      = '\!% '

set path=($path /home/local/Systems/scripts \
	/opt/nidas/bin /opt/arcom/bin \
	/usr/local/bin /sbin /usr/sbin /home/ads/bin \
	/home/ldm/bin /usr/lib64/qt4/bin)

unsetenv LD_LIBRARY_PATH

setenv MANPATH	/usr/share/man:/opt/local/man

alias cd        'set dot=$cwd; chdir'
alias ,         'set odot=$cwd; chdir $dot; set dot=$odot; pwd'

if ($?TERM && $OSTYPE == linux) then
  alias ls 'ls --color=tty'
  if ($AIRCRAFT == "Lab_N600" ) then
    set prompt      = "%{\033]0;%m:%~\007%}%m \!% "
  else
    set prompt      = "%{\033]0;%m:%~\007%}$AIRCRAFT \!% "
  endif
endif

alias ,		'set odot=$cwd; chdir $dot; set dot=$odot; pwd'
alias ..	'cd ..'
alias clr	'clear'
alias cls	'clear; ls'
alias cp	'cp -i'
alias ctagit	'ctags --extra=fq -eRo tags .'
alias dir	'ls -l \!* | more'
alias empty	'/bin/cp /dev/null'
alias ff	'find . -name \!* -print'
alias ftp	'ftp -i'
alias gerp	'grep'
alias h		history
alias hyper	ssh -Y ads@hyper.raf-guest.ucar.edu
alias herc	ssh -Y ads@hercules.raf-guest.ucar.edu
alias lman	'nroff -man \!* | $PAGER'
alias lookup	'grep \!* /usr/dict/words'
alias lpr4	'expand -4 \!* | lpr'
alias maek	'make'
alias mkae	'make'
alias mv	'mv -i'
alias pgrnd	'psql -h eol-rt-data.fl-ext.ucar.edu -d $PGGRND'
alias psg	'ps ax | grep \!*'
# ps which lists threads and real-time priority
alias psrt	'ps -eLo pid,tid,class,rtprio,ni,pri,psr,pcpu,stat,wchan:14,comm'
alias psrtg	'psrt | grep \!*'
alias scons     'nice scons -j4'
alias socsh	'source ~/.cshrc'
alias svn       '$HOME/bin/svn-ask-username.sh'
alias svndiff	'svn diff --diff-cmd /home/ads/bin/sh.svndiff'
alias vh	'$PAGER /usr/include/\!*.h'
alias vhdr	'$PAGER $JLOCAL/include/header.h'
alias vi	vim
#alias vpn	'/usr/local/bin/vpnclient connect ucar'
alias vpn	'sudo vpnc /etc/vpnc/eol.conf'
alias x		'chmod +x'
alias e		'xemacs -fg LightBlue -bg Maroon -geometry 90x46 &'

alias proj	'cd ${PROJ_DIR}/\!*'
alias fl	'cd /var/www/html/flight_data/\!*'
alias hp	'cd /home/local/aeros/source/\!*'
alias nids      'cd /home/local/nidas/src/\!*'
alias raf       'cd /home/local/raf'
alias dat	'cd $DATA_DIR/\!*'
alias rdat	'cd $RAW_DATA_DIR/$PROJECT\!*'

alias dumpiwg   'sudo tcpdump -i eth0 -A -s 1024 udp and dst port 7071'

set cdpath = ($PROJ_DIR/$PROJECT/$AIRCRAFT)
alias nerrt     'tail --lines=+0 --follow=name /var/log/ads3.log | grep --line-buffered -e WARN -e ERROR -e CRIT -e ALERT -e EMERG'

