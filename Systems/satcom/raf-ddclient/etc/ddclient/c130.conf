######################################################################
## 
## Define default global variables with lines like:
## 	var=value [, var=value]*
## These values will be used for each following host unless overridden
## with a local variable definition.
##
## Define local variables for one or more hosts with:
## 	var=value [, var=value]* host.and.domain[,host2.and.domain...]
##
## Lines can be continued on the following line by ending the line
## with a \
##
######################################################################
# daemon=30				# check every 300 seconds
syslog=yes				# log update msgs to syslog
mail=root				# mail all msgs to root
mail-failure=root			# mail failed update msgs to root
pid=/var/run/ddclient.pid		# record PID in file.
timeout=120
##
## dyndns.org dynamic addresses
##
## (supports variables: wildcard,mx,backupmx)
##
# IP address of members.dyndns.org is 63.208.196.96 as of 7/31/2007
# We pass -server in the runstring, with a numeric or character
# value based on whether dns is working or not.
# server=members.dyndns.org		
protocol=dyndns2			
# -host does not work in the runstring in ddclient-3.7.3,
# must be in config file
host=rafc130.dyndns.org
login=ChrisWebster				# default login
password=N130AR					# default password
