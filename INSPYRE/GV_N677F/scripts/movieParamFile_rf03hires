includeData = no
netcdfFile = <DATA_DIR>/INSPYRE/INSPYRErf03.nc

# Forward camera only. See movieParamFile.template for the four camera setup.
# A frame is created once a second from the earliest to the latest image time
# found in ANY of the image dirs below, so no single dir determines the times.
# A camera with no image at a frame time reuses its previous image if that
# image is recent enough; otherwise that part of the frame is left blank.
# See FRAME_INTERVAL and TIME_TOLERANCE at the top of combineCameras.pl.
imageDir1 = <RAW_DATA_DIR>/INSPYRE/camera/flight_number_rf03/forward
gravity1 = North
numCameras = 1

# For the moment, assume all cameras are the same type.
cameraName = Flea
movieDirectory = <RAW_DATA_DIR>/INSPYRE/Movies/hires
overlayImageTime = no
overlayImagePointing = no

# The outputResolution should be proportional to the scale:
# For one image per frame, leave the ht and add 200 to the width
# For two images, double the ht and add 50 or so padding,
#   and add 200 to the width
# For three images, double the ht and width (plus padding).
# One image, so outputResolution matches the scale below. outputResolutionD is
# only used when includeData = yes, and carries the extra 200 px of width for
# the flight data column.
scale = 1928x1214!
outputResolution=1928x1214
outputResolutionD=1928x1214
outputFrameRate = 15
mp4BitRate = 1500000
endParameters

GGALT
GGLAT
GGLON
ATX
PSXC
RHUM
TASX
THDG
PITCH
WSC
WDC
WIC
endVariablesLT
