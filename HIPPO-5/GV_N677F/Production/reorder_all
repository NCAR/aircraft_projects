#!/bin/csh

set user = `whoami`
if ($user == 'nimbus') then
	cd /scr/raf/local_productiondata
else
	cd ${DATA_DIR}
endif

foreach file ( HIPPO-5?f??.nc )
  echo $file
  ncReorder $file temp1.nc
  mv -f temp1.nc $file
end
